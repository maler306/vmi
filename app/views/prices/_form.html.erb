<%= simple_form_for(@path) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    
    <%= f.input :value, label: "Цена" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
